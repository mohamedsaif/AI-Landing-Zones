# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: azure-ai-landing-zone
metadata:
  template: azure-ai-landing-zone

infra:
  provider: bicep
  path: infra
  module: main
  parameters:
    file: infra/main.bicepparam